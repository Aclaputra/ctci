package ctci.arraynstrings;

import java.io.PrintStream;

public class CheckPermutation2 {
    static PrintStream out = System.out;
    public static boolean permutation(String s, String t) {
        /**
         * 1. if both arguments doesn't have same length then false.
         * 2. declare variable store index string into char array.
         * 3. declare variable store letters int array indexing how much per word appear.
         */
        return true;
    }
    public static void main(String[] args) {
        out.println(permutation("fis", "dog"));
        out.println(permutation("fsih", "fish"));
        out.println(permutation("king", "queen"));
    }
}